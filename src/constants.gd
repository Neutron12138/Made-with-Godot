class_name Constants extends Node

enum Team {
	PLAYER,
	ENEMY,
}

const FireballScene : PackedScene = preload("res://src/fireball.tscn")
const MonsterScene : PackedScene = preload("res://src/monster.tscn")
